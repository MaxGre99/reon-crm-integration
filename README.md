# Задача: Работа с карточками “Контакт” и “Сделка” и хуками amoCRM.

1. Интеграция будет принимать от amo хук на установку, получать токены и сохранять в базе аккаунт, если есть обновлять. При хуке на удаление значения токенов должны быть null и isInstalled быть false. Также должен быть реализован метод обновления токенов по Cron, который будет каждые 12 часов обновлять всем аккаунтам токены. Для работы с amo api нужно реализовать отдельный модуль.

### Модель аккаунта должна содержать следующие поля:

- accountId (id аккаунта в amo, это поле уникальное)
- subdomain (subdomain аккаунта в amo)
- accessToken (access token для доступа к api amo)
- refreshToken (refresh token для обновления accessToken)
- isInstalled (boolean значение, установлен ли виджет в аккаунте или нет)

2. После этого надо выгрузить из amo кастомные поля и проверить, есть ли там нужные нам, все поля которых нет, надо создать, затем сохранить все нужные нам поля в базу данных и сохранить связь с моделью Аккаунт в базе данных, т.к. у каждого аккаунта свои id кастомных полей.

### “Контакт” должен иметь кастомные поля:

- Дата рождения (тип поля в amo “Дата”)
- Возраст (тип поля в amo “Число”)
- Лазерное омоложение лица (тип поля в amo “Число”)
- Ультразвуковой лифтинг (тип поля в amo “Число”)
- Лазерное удаление сосудов (тип поля в amo “Число”)
- Коррекция мимических морщин (тип поля в amo “Число”)
- Лазерная эпиляция (тип поля в amo “Число”)

### “Сделка” должна иметь кастомные поля:

- Услуги (тип поля в amo “Мультисписок”)
- Бюджет (стандартное поле amo, тип “Число”)

Модель кастомного поля должна содержать следующие данные:

- fieldType (содержит тип поля в amo)
- fieldId (содержит id поля в amo)
- fieldName (уникальное, названия указываются в проекте в константах, по названиям и типу мы проверяем, наличие нужных полей в amo, а также название нужно чтобы достать поле из базы)
- accountId (id аккаунта в нашей базе)

3. Посредством amo api проверить стоят ли вебхуки в amo на события: “Контакт добавлен”, “Контакт изменен”, “Сделка добавлена”, “Сделка изменена”, если нет установить.

4. Добавить модуль работы с контактами, со следующим функционалом:
   При получении хука на добавление или изменение контакта, проверяется заполнено ли поле “Дата рождения” и если не заполнено, то ничего не делаем иначе подсчитываем возраст и обновляем поле “Возраст”, учесть что тк мы изменим поля контакта, нам снова придет вебхук о изменении

5. Добавить модуль работы со сделками, со следующим функционалом:
   При получении хука на добавление или изменение сделки проверяем выбраны ли услуги в кастомном поле, если да, то получаем данные привязанного к сделке основного контакта (если есть) и смотрим заполнены ли у него поля нужных нам услуг.
    1. если есть незаполненные поля то надо создать задачу (id задачи типа “ошибка” должен быть определен в env) с текстом “У контакта не заполнены поля услуг: [перечислить выбранные услуги у которых не заполнены поля]” (если такая задача уже есть, то мы не создаем, а обновляем ее текст если требуется), дедлайн задачи 24 часа

    2. если не заполнено поле возраст, то посмотреть можем ли мы его заполнить
       если да, то вызвать алгоритм обработки возраста контакта
       если нет, то надо создать задачу (id задачи типа “ошибка” должен быть определен в env, если такая задача есть, то ничего не делаем) с текстом “Возраст контакта неизвестен”, дедлайн 24 часа

    3. если все поля заполнены, то подсчитываем стоимость услуг, обновляем поле “Бюджет” и ставим задачу (id задачи типа “проверить” должен быть определен в env, если такая задача есть, обновляем если требуется) с текстом “Проверить стоимость услуг для [Название контакта], возраст: [Возраст контакта]”, дедлайн 24 часа

Все задачи создаются для карточки “Сделка”. ID для задач должны быть определены в env, для тестов можно использовать: дефолтные амовские id 0 и 1; или самому создать в амо нужные типы задач и использовать их id

6. Создать папку docker-compose и в ней файл для поднятия приложения и базы, также для docker-compose файла надо описать env example, которй будет использоваться для запуска и содержать все данные нужные как для запуска приложения так и для запуска базы. У базы должен быть определен volume, чтобы данные не терялись.

## Стек технологий:

фреймворк NestJS
база: MongoDB (ORM: mongoose) или PostgreSQL (ORM: TypeORM)
docker

## Порядок и правила выполнения:

- вся разработка происходит согласно правилам разработки backend приложений, пункты: “NEW Правила оформления кода Backend” и “NEW Структура Backend проекта”, также там есть описание работы с github

- на github создается репозиторий c веткой main, от main создается ветка development, в development делается init commit с чистым NestJS проектом

задача выполняется по пунктам (они пронумерованы от 1 до 6), один пункт это один Pull Request(далее PR), каждый пункт выполняется в отдельной ветке git и после проверки PR, мерджится в development и от development создается новая ветка со следующей задачей.


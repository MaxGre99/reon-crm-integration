export const AmoCustomFieldTypes = {
    Date: 'date',
    Numeric: 'numeric',
    Multiselect: 'multiselect',
} as const;

export const AmoEntityTypes = {
    Contacts: 'contacts',
    Leads: 'leads',
} as const;
